<tars>
    <application>
        <server>
            app=Hello
            server=HelloServer
            local=tcp -h 127.0.0.1 -p 18014 -t 30000
            logpath=/tmp
            <Hello.HelloServer.HelloWorldObjAdapter>
                allow
                endpoint=tcp -h 127.0.0.1 -p 18015 -t 60000
                handlegroup=Hello.HelloServer.HelloWorldObjAdapter
                maxconns=200000
                protocol=tars
                queuecap=10000
                queuetimeout=60000
                servant=Hello.HelloServer.HelloWorldObj
                threads=1
            </Hello.HelloServer.HelloWorldObjAdapter>
        </server>
    </application>
</tars>
